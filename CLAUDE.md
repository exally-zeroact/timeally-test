# Timeally（タイマリー）— このrepoの決まり

タイムカード。**打刻の生データは1分単位で残す（丸めない・上書きしない・消さない）**。

---

## ★このrepoはどっちか★

**フォルダ名を環境の証拠にしない。`remote` と `js/supa-config.js` の `env` を見る。**
（実際に `Exally-test` という名前のフォルダの中身が **代行請求の本番** だった事故がある）

| repo | 倉庫 | `env` | 配信 |
|---|---|---|---|
| `timeally-test` | DB-test（他アプリと共有のテスト倉庫）の `timeally` 部屋 | `test` | Vercel → **https://timeally-test.vercel.app** |
| `timeally` | 本番倉庫の `timeally` 部屋 | `prod` | Vercel（プロジェクトは作成ずみ・**まだ push していない**） |

**配信は Vercel。GitHub Pages は使わない。**
`exally-zeroact.github.io/…` は **URL に Exally の名前が出る**（github.io を使っているのは
うちで Exally だけ。他は全部 `*.vercel.app`）。**Timeally は別アプリなので混ぜない。**

**`js/supa-config.js` をもう片方の repo にコピーしない。** これ1本が環境の分かれ目。

---

## ★触る前に★

1. `git fetch` する（古いまま作業すると「手元は緑・CIは赤」になる）
2. **数字を出すときは `origin/main` で確認する**（手元の作業ツリーを根拠にしない）
3. 新しい画面・部品・出力を作る前に **社内に同じ物が無いか探す**（`VENDOR.md` を見る）

---

## ★守る決まり★

### 法律（実装前に一次情報で再確認する）
- **労働時間は1分単位が原則**（労基法24条 賃金全額払い）。**日ごとの切り捨ては違法**
- 認められる例外は1つ＝**1か月合計の時間外・休日・深夜の端数**（昭和63.3.14 基発150号）
- **法定の率・額を説明文に直書きしない。** すべて `lib/tc-law.js`（出典URL＋確認日つき）
- 会社が選べるのは見せ方だけ：`none`（既定・適法）／`month`（適法）／`daily30`（客の希望・適法ではない）
- **`daily30` のときは切り捨てた時間と金額を必ず記録して社長に見せる**

### 従業員の画面
- **「打つだけ」。数えた結果（働いた長さ・残業・丸め・金額・法律の話）を1つも出さない**
- **丸めの説明文も出さない**
- 出すのは**自分が打った1分単位の生の時刻だけ**＝**従業員は嘘の数字を一度も見ない**
- 記録は**社長側と帳簿に必ず残す**
- 守り方は3重：①RPCが時刻しか返さない ②画面が数える道具を読まない ③`tests/employee-screen.test.mjs`

### 数える所
- **潰す関数は `lib/tc-calc.js` の `summarize()` 1本だけ。** 画面・印刷・CSV・Excel・気づきは全部これを見る
- `lib/` に `Date.now()` / `new Date()` を書かない（毎回同じ答えになるように）

### 配色（承認済み・変更禁止）
```
#FFC72C 主＝面   #F0B400 縁・押した時   #8F6200 濃＝文字   #FFE08A 明＝選択中
#FFFBF0 淡背景   #F0E0B8 淡境界        #78705C 補助文字   #2B2418 黄の上の文字
＋ #FFFFFF（白） #B3261E（注意・警告の赤）
```
- **黄を文字色に使わない**／**黄の面の上は黒い文字（白文字は禁止）**
- **注意の橙を使わない**（主色と混ざる）。注意も警告も**赤に寄せる**
- **CSS変数は使わない（直hex）**／**全アプリ禁止の濃い緑は使わない**
- **入力欄は16px**（小さいと iPhone が勝手に拡大してスクロールが壊れる）

### 倉庫
- 実の棚は `timeally.*`。アプリは **`public` の窓口(view)ごし**にしか読まない
- **窓は `security_invoker = true` が命綱**。`create or replace view` は落とすので**直後に `alter view`**
- **列を足す順番：① 部屋の表 → ② 窓を作り直す → ③ コード配信**（窓を忘れると丸ごと落ちる）
- **表は5つだけ。増やさない**
- 当てるのは `npm run db:apply`（門番 `scripts/sql-guard.mjs` を必ず通る）
- **本番倉庫に当てるのは司さんの一言があってから**（`--yes-prod` が要る作りにしてある）

### 出す物
- **ファイルの渡し口は `js/file-out.js` だけ**（種類を正しく付けないと iPhone で開けない）
- **保存名は中身から作って、押す前に画面に出す**（`lib/tc-name.js`）
- **印刷は紙だけの新しい窓で刷る**／**中身が0枚なら開かない**（白紙のダイアログを出さない）
- **紙に「どう絞り込んだか」を刷らない**

---

## ★出す前に走らせる★

```
npm install
node tests/run.js          # 全部（自己確認 --self-test 込み）
node scripts/stamp-build.mjs   # ?v= を貼り直す（差分もコミットする）
```

- **テストを足したら `.github/workflows/ci.yml` に並べる。** 並べないと**1本も走らない**
  （`tests/ci-coverage.test.mjs` が並べ忘れを赤にする）
- **`--self-test`（わざと壊して赤になるか）も一緒に並べる**
- **`[skip ci]` と書かない**（Vercelのデプロイまで止まる）

---

## ★やらない★

- **本番への push は、実スクショを見せて OK をもらってから**
- **クラウドのデータを指示なく触らない**
- **実配信を叩き続けない**（403で自分のテスト環境が止まる）。確認は間を空けて1回
- **`SITE_URL`（Supabase Auth）を触らない**。全アプリのログイン戻り先が壊れる
  （**新しいURLを Redirect URLs に足す**のは必要。足さないと「ログインしたら請求書アプリに飛ぶ」）
